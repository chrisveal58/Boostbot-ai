import React from "react";
import ReactDOM from "react-dom/client";
import BoostBotApp from "./App";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <BoostBotApp />
  </React.StrictMode>
);
